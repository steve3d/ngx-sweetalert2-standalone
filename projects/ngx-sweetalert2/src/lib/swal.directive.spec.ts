import { SwalDirective } from './swal.directive';

describe('SwalDirective', () => {
  it('should create an instance', () => {
    const directive = new SwalDirective();
    expect(directive).toBeTruthy();
  });
});
